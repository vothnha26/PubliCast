const { TOKEN_REFRESH } = require('../../../../utils/constants');

const TOKEN_REFRESH_CONSTANTS = {
  LOOKHEAD_DAYS: parseInt(process.env.TOKEN_REFRESH_LOOKHEAD_DAYS, 10) || TOKEN_REFRESH.LOOKHEAD_DAYS,
  SCHEDULER_INTERVAL_MS: parseInt(process.env.TOKEN_REFRESH_INTERVAL_MS, 10) || TOKEN_REFRESH.SCHEDULER_INTERVAL_MS,
  STARTUP_DELAY_MS: parseInt(process.env.TOKEN_REFRESH_STARTUP_DELAY_MS, 10) || TOKEN_REFRESH.STARTUP_DELAY_MS
};

module.exports = { TOKEN_REFRESH_CONSTANTS };
